phase: tunning

tracker:
  project_name: aprendizagem_maquina_projeto

data_loader:
  name: ionosphere

pre_processing:
  name: none

modeling:
  structure:
    name: bayes_class
  
  training:
    name: stratified_k_fold_tunning_train
    tunning_config: tune-bayes_class
    n_repeats: 30

metrics:
  name: bayes_classifier_metrics
